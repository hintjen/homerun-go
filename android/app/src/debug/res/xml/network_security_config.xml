<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. The release manifest never references this file, so
  nothing here can reach a shipped app.

  It exists for one job: standing a fake manifest endpoint up on the developer's
  machine while the real one is being written. `10.0.2.2` is the emulator's
  route to the host, and `localhost` is a phone reached over `adb reverse`.

  Deliberately *not* a blanket `cleartextTrafficPermitted="true"`. Even in a
  debug build, allowing cleartext everywhere would let a plain-HTTP mistake
  against the real API pass unnoticed here and fail only in release.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
